// import { loadStripe } from "@stripe/stripe-js";
// let stripePromise;
// const getStripe = () => {
//   if (!stripePromise) {
//     stripePromise = loadStripe(process.env.next_PUBLIC_STRIPE_PUBLISHABLE_KEY);
//   }
//   return stripePromise;
// };

// export default getStripe;

// rearrange sanity.js, getStripe and confetti in lib folder
//  rerrange folder structure
//  accumulate all tailwind styles in globall.css
// create layout
